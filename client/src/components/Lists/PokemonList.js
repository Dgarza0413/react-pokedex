import React from 'react'

const PokemonList = () => {
    const randomGridList = () => {
        // [...Array(20)].map(e=> e)
    }
    return (
        <div>

        </div>
    )
}

export default PokemonList